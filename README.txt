Compile with g++ [mainFile].cpp -lgdi32

-lgdi32: Links gdi32 library


main is compiled and executed -> on button click -> instantiate image(image.h) class -> it takes screenshot, saves to BMP image, reads pixel data -> call OCR (ocr.h) algorithm to parse the data -> ???